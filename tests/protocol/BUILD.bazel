load("@rules_rust//rust:defs.bzl", "rust_test")

# External proof test: exercises the Bazel-generated Cap'n Proto bindings
# through the public `artisan_protocol` surface, not crate internals.
rust_test(
    name = "phase1_proof_test",
    srcs = ["phase1_proof.rs"],
    crate_name = "phase1_proof_test",
    deps = [
        "//modules/protocol:protocol",
        "@crates//:capnp",
    ],
    edition = "2024",
    lint_config = "//:workspace_lints",
    size = "small",
    visibility = ["//:__pkg__"],
)

# Phase 2 product schema test: every message family of
# `modules/protocol/schema/artisan.capnp` round-trips through the generated
# bindings, still via the public `artisan_protocol` surface.
rust_test(
    name = "product_roundtrip_test",
    srcs = ["product_roundtrip.rs"],
    crate_name = "product_roundtrip_test",
    deps = [
        "//modules/domain:domain",
        "//modules/protocol:protocol",
        "@crates//:capnp",
    ],
    edition = "2024",
    lint_config = "//:workspace_lints",
    size = "small",
    visibility = ["//:__pkg__"],
)

# Phase 2 conversation replay schema test: every conversation Request /
# Response / Envelope arm, the bounded snapshot and patch shapes, all five
# patch variants, and malformed representability for the owned-conversion
# packet to reject. Raw wire only; no owned conversions here.
rust_test(
    name = "conversation_roundtrip_test",
    srcs = ["conversation_roundtrip.rs"],
    crate_name = "conversation_roundtrip_test",
    deps = [
        "//modules/domain:domain",
        "//modules/protocol:protocol",
        "@crates//:capnp",
    ],
    edition = "2024",
    lint_config = "//:workspace_lints",
    size = "small",
    visibility = ["//:__pkg__"],
)

rust_test(
    name = "conversation_codec_test",
    srcs = ["conversation_codec.rs"],
    crate_name = "conversation_codec_test",
    deps = [
        "//modules/domain:domain",
        "//modules/protocol:protocol",
        "@crates//:capnp",
    ],
    edition = "2024",
    lint_config = "//:workspace_lints",
    size = "small",
    visibility = ["//:__pkg__"],
)

rust_test(
    name = "owned_codec_test",
    srcs = ["owned_codec.rs"],
    crate_name = "owned_codec_test",
    deps = [
        "//modules/domain:domain",
        "//modules/protocol:protocol",
        "@crates//:capnp",
    ],
    edition = "2024",
    lint_config = "//:workspace_lints",
    size = "small",
    visibility = ["//:__pkg__"],
)

# Assistant conversation-item test: the appended assistantMessage union arm
# round-trips through the production owned codec, and every validated
# assistant field rejects malformed raw input with typed errors.
rust_test(
    name = "assistant_message_test",
    srcs = ["assistant_message.rs"],
    crate_name = "assistant_message_test",
    deps = [
        "//modules/domain:domain",
        "//modules/protocol:protocol",
        "@crates//:capnp",
    ],
    edition = "2024",
    lint_config = "//:workspace_lints",
    size = "small",
    visibility = ["//:__pkg__"],
)

rust_test(
    name = "conversation_activity_test",
    srcs = ["conversation_activity.rs"],
    crate_name = "conversation_activity_test",
    edition = "2024",
    lint_config = "//:workspace_lints",
    size = "small",
    visibility = ["//:__pkg__"],
)
