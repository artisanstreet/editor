/** Identifies the one-shot exec JSONL protocol independently from app-server stdio. */
export const codex_exec_transport = "codex-exec-jsonl";

/** Identifies the normalized protocol version for current exec JSONL events. */
export const codex_exec_protocol_version = "codex-exec-jsonl-v1";
