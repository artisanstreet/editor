load("@rules_rust//rust:defs.bzl", "rust_library")

exports_files(["Cargo.toml"])

rust_library(
    name = "database",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "artisan_database",
    deps = [
        "//modules/domain:domain",
        "@crates//:sea-orm",
        "@crates//:sqlx-sqlite",
        "@crates//:thiserror",
    ],
    edition = "2024",
    lint_config = "//:workspace_lints",
    visibility = ["//visibility:public"],
)
