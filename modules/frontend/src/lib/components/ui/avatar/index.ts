import Root from "./avatar.svelte";
import Image from "./avatar-image.svelte";
import Fallback from "./avatar-fallback.svelte";

export { Root, Root as Avatar, Image, Image as AvatarImage, Fallback, Fallback as AvatarFallback };
