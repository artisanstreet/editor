<script lang="ts">
	/**
	 * The application opens on a new thread.
	 *
	 * No project is named in this URL, so the sentence above the composer is what
	 * names one — seeded with wherever the reader was last, and changeable in
	 * place. Everything else about the surface is identical to entering a project
	 * by its own route.
	 */
	import NewThreadRoute from "./components/new-thread-route.svelte";
</script>

<NewThreadRoute />
