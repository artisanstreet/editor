# Backend Completion Matrix

Scope: the active objective is the complete backend/core and testing harness described by `artisan-editor-prd.md`. Status is implementation status as of 2026-07-10, not design intent. `missing` means no production implementation exists in this repository; a research or PRD entry is evidence of a gap, not completion.

## Status Legend

- `implemented`: the scoped requirement exists and has a direct verification path.
- `partial`: a narrow slice exists, but the PRD contract is broader or incomplete.
- `missing`: no usable implementation or test seam exists yet.

## Protocol / Persistence

| Requirement                                                                                                                               | Status      | Authoritative evidence                                                                                                                                                                                        | Verification gate                                                                                                                                                         |
| ----------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Shared, transport-safe Effect Schema module with inferred protocol types and no runtime adapter dependencies                              | implemented | `modules/protocol/src/messages.ts`; `modules/protocol/src/codec.ts`; PRD `245-253`, `359-362`                                                                                                                 | Compile an import-boundary test proving protocol imports do not reach Electron, Node side-effect adapters, or Drizzle; encode/decode fixtures pass.                       |
| Versioned envelopes covering hello, command, receipt, query/result, subscription/snapshot/patch, event, stream, ACK, heartbeat, and error | partial     | Current envelope union only has command, receipt, event, and error in `modules/protocol/src/messages.ts`; PRD `364-377`, `387-410`                                                                            | Contract fixtures exercise every envelope kind, version negotiation, malformed payload rejection, and exhaustive routing.                                                 |
| Trace metadata, correlated receipts, stable error codes, and idempotent command acceptance                                                | partial     | `modules/protocol/src/messages.ts`; `modules/backend/src/protocol/protocol-router.ts`; `modules/backend/src/persistence/journal-store.ts`; `.tests/backend/protocol-router.test.ts`; PRD `288-325`, `436-443` | Retry the same command after restart, mutate its intent, assert one durable acceptance, prior receipt/event replay, correlation/causation, and retryability.              |
| SQLite WAL database, Drizzle Effect 4 driver, migrations, and persistence behind a service boundary                                       | implemented | `modules/backend/src/persistence/database.ts`; `modules/backend/drizzle/20260710061100_cloudy_squirrel_girl/migration.sql`; `modules/backend/src/runtime/backend-runtime.ts`; PRD `445-462`                   | Start against a fresh database, apply migrations, verify WAL mode, restart safely, and run persistence tests without exposing Drizzle types through protocol/domain APIs. |
| Canonical event ledger, command deduplication, per-stream sequencing, and synchronous projection updates for all domain commands          | partial     | `modules/backend/src/persistence/schema.ts`; `modules/backend/src/persistence/journal-store.ts`; `modules/backend/src/threads/thread-commands.ts`; only `thread.create` is routed                             | Add every domain command and projection repository; transaction tests prove ledger, dedupe, stream cursor, and projection commit or rollback together.                    |
| Replay, gap detection, event ACKs, reconnect resume, snapshots, and projection rebuild from the ledger                                    | missing     | No replay/query/subscription service in `modules/backend/src`; PRD `312-325`, `404-410`, `436-443`                                                                                                            | Reconnect with a last-applied cursor and prove ordered replay or snapshot fallback; delete projections, rebuild them, and compare observable state.                       |

## Engines / Codex

| Requirement                                                                                                                           | Status  | Authoritative evidence                                                                  | Verification gate                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------- | ------- | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| Canonical `Engine` interface, explicit capabilities, registry, and Effect-layer composition                                           | missing | No `modules/engines/` directory; PRD `165-196`, `173-190`                               | Compile a fake and Codex adapter through one interface; startup rejects duplicate ids, missing layers, and unsupported capability assumptions.      |
| Codex adapter using `codex app-server`, with `codex exec --json` fallback, process control, and raw-origin retention                  | missing | `agent-engine-io-research.md` is research only; no Codex source exists; PRD `1974-1985` | Fixture and opt-in live tests cover startup, resume, approvals, steering, cancellation, failures, final results, and preserved raw frames.          |
| Engine output normalization into Artisan events and canonical surfaces                                                                | missing | No engine normalizer or event model beyond `thread.created`; PRD `527-555`, `618-632`   | Replay representative Codex frames and assert normalized events, raw origin, stable correlation, and no provider nouns in the public surface model. |
| Engine lifecycle support for discovery, auth state, start, resume, stream, steer, approve, cancel, crash, timeout, and orphan cleanup | missing | No engine/process adapter implementation; PRD `192-196`, `227-237`                      | Shared conformance scenarios pass against fake child process, transcript replay, and compatible live adapter where enabled.                         |

## Orchestration

| Requirement                                                                                                                                   | Status  | Authoritative evidence                                                                                                         | Verification gate                                                                                                                                                               |
| --------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| First-class `AgentOrchestrator` owning assignment, scope, policy, lifecycle, steering, stop/pause/resume/close, joins, conflicts, and usage   | missing | No orchestrator module; `modules/backend/src` contains only `threads`, `protocol`, `persistence`, and `runtime`; PRD `634-657` | Start a fan-out group through the public protocol, steer and stop workers, join results, and assert durable roster/projection state.                                            |
| Durable agent identity, role, display name bank, native identity metadata, artifacts, and status heartbeat                                    | missing | No agent schema or projection; PRD `659-696`, `727-743`                                                                        | Roster tests prove unique display names, role separation, exhaustion fallback, provider metadata preservation, status updates, and no chain-of-thought leakage.                 |
| Intake clarification, assumptions, live steering versus queued messages, and hybrid harness policy                                            | missing | No harness policy/service; PRD `545-575`, `585-632`                                                                            | Ambiguous/high-risk input emits `Question` before execution; low-risk ambiguity records an assumption; steering tests cover capable, disabled, unsupported, and rejected paths. |
| Canonical surface layer and event model for Work, Time, Guidance, Routines, Capabilities, Processes, Changes, Permissions, and native actions | missing | No surface taxonomy or normalized action model; PRD `470-514`, `618-632`                                                       | Surface contract tests map engine-owned and Artisan-owned actions to one event shape while retaining raw origin metadata.                                                       |

## Terminal / Process / Files / Git / Preview

| Requirement                                                                                                                  | Status  | Authoritative evidence                                                                                                               | Verification gate                                                                                                                                                    |
| ---------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Terminal and process orchestration with ownership, output streaming, input, restart, kill, exit, failure, and cleanup        | missing | No terminal/process module or protocol payload; `modules/protocol/src/messages.ts`; PRD `585-600`, `2048-2049`                       | Spawn a real child process in a temporary workspace, stream fragmented stdout/stderr, exercise input/restart/kill, and assert terminal state plus no leaked handles. |
| File read/write, changed-file tracking, diffs, approvals, and review projections                                             | missing | No filesystem/file/diff module; only `Threads` projection in `modules/backend/src/persistence/schema.ts`; PRD `597-600`, `2052-2055` | Agent edits appear in the changed-files projection and ledger without opening tabs; approval and rollback tests cover risky writes.                                  |
| Git status, branch/worktree, diff summary, and session consistency                                                           | missing | No Git service or command types; PRD `596-599`, `2054-2055`                                                                          | Temporary Git repository scenarios prove branch/diff/status effects, event ordering, and recovery after process failure.                                             |
| Preview targets, local preview inspection, link metadata, redirect handling, and private/local URL blocking                  | missing | No preview or link backend; PRD `599`, `804-839`, `2073-2075`                                                                        | Backend fetch tests assert metadata fallback, cache behavior, blocked targets, redirect preservation, and preview lifecycle events.                                  |
| Artisan-owned control-plane tools for questions, assumptions, terminals, files, Git, previews, approvals, and native actions | missing | No tool registry or MCP control-plane implementation; PRD `585-632`, `1920-1927`                                                     | Tool registry contract tests prove eligibility, approval policy, observable invocation events, and equivalent normalization for engine-owned actions.                |

## Settings / Retention / Guidance / Project Affinity

| Requirement                                                                                                                        | Status  | Authoritative evidence                                                                                                                                     | Verification gate                                                                                                                                                              |
| ---------------------------------------------------------------------------------------------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Thread metadata projection with title source/lock, live status, current goal, rename suggestion, and activity updates              | partial | `modules/backend/src/persistence/schema.ts` stores only title and timestamps; `modules/backend/src/threads/thread-commands.ts`; PRD `870-929`, `1943-1947` | Metadata tests prove initial title, auto-refinement, user rename lock, pivot updates, activity timestamps, and live status projection.                                         |
| Default-enabled seven-day inactive-thread retention, toggle, pinned exemption, startup cleanup, and periodic cleanup               | missing | No retention fields, scheduler, or cleanup service; PRD `840-868`, `2076-2078`                                                                             | Time-controlled tests cover default 7-day deletion, activity reset, toggle-off, pinned/archive behavior, startup, and periodic cleanup.                                        |
| Canonical global guidance source, precedence, provider sync status, runtime fallback, first-run import, backup, and drift handling | missing | No settings/guidance module; PRD `1098-1161`, `1903-1916`, `2104-2108`                                                                                     | Import/sync tests cover zero/one/multiple provider values, dedupe, backup before overwrite, precedence, drift, and `synced`/`applied_at_run_time`/`unsupported`/`sync_failed`. |
| Project affinity scoring, linked projects, locked placement, high-confidence rehome, and medium-confidence suggestion              | missing | No project fields or affinity service; PRD `1051-1097`, `1948-1956`                                                                                        | Multi-repo scenarios prove high-confidence auto-rehome, medium-confidence suggestion, linked-project retention, and locked-project protection.                                 |

## Marketplace Skills / MCP

| Requirement                                                                                                                                        | Status  | Authoritative evidence                                                        | Verification gate                                                                                                                                                            |
| -------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Canonical Routine/skill registry with source, version, scope, permissions, compatibility, files, enable/disable/remove, and progressive disclosure | missing | No marketplace or registry module; PRD `1163-1267`, `2112-2119`               | Registry tests cover local/Git/catalog/provider imports, global/workspace/project scope, filtering, lifecycle, eligibility, and ledgered invocation.                         |
| Skill install approval, preview, rollback, provider sync, drift detection, and runtime-only fallback                                               | missing | No install/sync/approval service; PRD `1183-1200`, `1243-1267`                | Agent-requested install shows source/files/permissions/trust/scope/rollback before writing; sync tests cover supported and unsupported engines plus drift.                   |
| Canonical MCP Capability registry for stdio and HTTP transports, auth/secrets, health, tools/resources, scope, and lifecycle                       | missing | No MCP module or process manager; PRD `1279-1347`, `2120-2123`                | Registry and lifecycle tests cover stdio launch/crash/restart/uninstall and HTTP auth/OAuth/token/refresh/disconnect states.                                                 |
| MCP tool policy, approval, secure connection handling, sync/drift, progressive disclosure, and invocation ledger events                            | missing | No capability policy or approval implementation; PRD `1317-1370`, `2124-2130` | No MCP starts or connects silently; approval fixtures expose origin/auth/network/tools/trust, enforce per-tool policy, log calls, and surface failures as capability events. |

## Electron Transport

| Requirement                                                                                                                            | Status  | Authoritative evidence                                                                                                                         | Verification gate                                                                                                                                         |
| -------------------------------------------------------------------------------------------------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Shell-neutral typed session interface with Electron as an adapter, not a backend dependency                                            | partial | In-process `ProtocolRouter` and `make_backend_runtime` exist in `modules/backend/src`; no Electron/session adapter; PRD `245-347`, `1986-1996` | Import-boundary test proves frontend/backend/core do not import Electron IPC; an adapter drives the same session interface.                               |
| V1 control and stream MessagePorts with version negotiation, cursors, snapshots, patches, bounded buffering, and binary stream support | missing | No Electron module, MessagePort adapter, or stream envelope; PRD `352-443`                                                                     | MessagePort contract fixtures run control and stream traffic, preserve ordering, and prove stream load cannot delay cancel/approve/steer.                 |
| Reconnect, heartbeat, event ACK, durable receipt timing, replay, and in-process transport equivalence                                  | missing | `modules/backend/src/protocol/protocol-router.ts` has no connection lifecycle; PRD `304-325`, `424-443`                                        | Run identical fixtures through MessagePort and in-process transports; disconnect/reconnect tests prove heartbeat failure, replay, and idempotent retries. |

## Testing / Release

| Requirement                                                                                                                                                           | Status      | Authoritative evidence                                                                                                                     | Verification gate                                                                                                                                                |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Baseline protocol and backend tests for schema rejection, durable create, duplicate retry, conflict, ordering, and malformed input                                    | implemented | `.tests/protocol/codec.test.ts`; `.tests/backend/protocol-router.test.ts`; `vitest.config.ts`                                              | `corepack pnpm run validate` remains green; current baseline is 2 files and 12 tests.                                                                            |
| Engine Conformance Harness outside production source with fake child process, byte-faithful replay, and opt-in live backend                                           | missing     | No `.tests/engines/` tree; PRD `198-225`, `2008-2014`                                                                                      | The same scenario runs against fake process, recorded transcript, and explicitly enabled live CLI without paid tests running in ordinary CI.                     |
| Scenario coverage for fragmentation, interleaving, approvals, steering, cancellation, resume, crash, timeout, malformed/duplicate frames, restart, and orphan cleanup | missing     | No engine scenarios or fixtures; PRD `227-243`, `2012-2017`                                                                                | Scenario matrix passes and asserts canonical events, final projections, durable effects, and cleanup invariants.                                                 |
| Deep backend integration harness with temporary workspace, Git repo, SQLite, deterministic clock/ids, fake engine, and public protocol router                         | partial     | Current temporary SQLite and public-router tests in `.tests/backend/protocol-router.test.ts`; no workspace/Git/process/fake-engine harness | One end-to-end scenario proves only observable protocol output, ledger records, rebuilt projections, filesystem/Git effects, and process cleanup.                |
| Property/state-machine tests for idempotency, stream continuity, replay equivalence, lifecycle terminality, correlation, and resource cleanup                         | missing     | No generated action or state-machine tests; PRD `239-243`, `2018-2020`                                                                     | Generated action sequences preserve invariants across retries, exits, reconnects, and projection rebuilds with zero leaked processes/handles.                    |
| Protocol, persistence, router, transport-load, domain, architecture-boundary, and surface-layer contract suites                                                       | partial     | `package.json` has local format/lint/type/test scripts; current tests cover only codec and thread create; PRD `2023-2047`, `2136-2149`     | Add contract fixtures for all envelope/domain kinds, migrations/WAL, transport equivalence/load, handler ownership, import boundaries, and normalization.        |
| CI/release gate for full backend and harness validation, fixture policy, and opt-in live tests                                                                        | missing     | No workflow/release configuration in the repository; `package.json` only defines local `validate`; PRD `2002-2157`                         | CI runs format, lint, typecheck, unit, conformance, integration, property, and release checks; live CLI tests require an explicit opt-in and never run silently. |

## Dependency-Ordered Milestones

| Milestone                                                                                                                       | State   | Dependencies   | Exit gate                                                                                                                        |
| ------------------------------------------------------------------------------------------------------------------------------- | ------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| M0. Stabilize the current protocol/persistence foundation and expose test seams                                                 | partial | None           | All current behavior has explicit services, deterministic metadata injection, projection/replay seams, and green baseline tests. |
| M1. Complete the wire protocol, envelope/domain routers, ACKs, cursors, replay, snapshots, and projection repositories          | pending | M0             | Protocol fixtures pass for every required kind and reconnect/replay is observable through the public router.                     |
| M2. Implement terminal/process, filesystem/diff, Git, preview, approval, and Artisan tool services                              | pending | M1             | Temporary workspace/Git/process scenarios prove side effects, ownership, review state, and cleanup.                              |
| M3. Add the Engine interface, explicit registry/layers, Codex app-server adapter, fallback, and normalization                   | pending | M1, M2         | Codex adapter passes the shared lifecycle contract and preserves raw origin metadata.                                            |
| M4. Build the fake child-process, transcript-replay, live opt-in, and shared conformance harness                                | pending | M3             | Every engine scenario runs through all compatible harness backends and enforces cleanup invariants.                              |
| M5. Add harness policy and `AgentOrchestrator` for clarification, assumptions, steering, fan-out, joins, and roster projections | pending | M2, M3, M4     | End-to-end multi-agent scenarios prove canonical statuses, identity mapping, steering, stop/close, and joins.                    |
| M6. Add thread metadata, retention, activity state, global guidance, and project affinity services                              | pending | M1, M5         | Time-controlled and multi-project tests pass, including locks, drift, rehome, and guidance precedence.                           |
| M7. Add canonical Routine/skill and MCP Capability registries with approvals, sync, drift, health, and ledger events            | pending | M2, M3, M5, M6 | Install/connect/invoke/rollback tests pass for scope, trust, compatibility, policy, and provider mirrors.                        |
| M8. Add the Electron shell adapter with control/stream MessagePorts and a typed session bridge                                  | pending | M1-M7          | MessagePort and in-process fixtures have equivalent observable behavior under reconnect and stream load.                         |
| M9. Run deep integration, property/state-machine, architecture-boundary, and transport-load suites                              | pending | M4, M5, M7, M8 | Generated and end-to-end suites prove no duplicate effects, gaps, leaked resources, or transport starvation.                     |
| M10. Wire the full CI/release gate and opt-in live fixture policy                                                               | pending | M9             | Clean checkout passes the complete gate; live tests are explicit, sanitized, and excluded from ordinary CI by default.           |

## Final Completion Audit

- [ ] Every PRD envelope kind, domain command, query, subscription, ACK, heartbeat, stream, and error has an Effect Schema, router, fixture, and compatibility test.
- [ ] Every accepted command is durable, idempotent, correlated, replayable, and projected transactionally; projections rebuild from the ledger.
- [ ] Engine registry and capabilities are explicit; Codex app-server, fallback, normalization, raw capture, and lifecycle behavior pass conformance.
- [ ] Fake process, transcript replay, and opt-in live harnesses pass the shared scenario matrix without paid tests in ordinary CI.
- [ ] AgentOrchestrator owns visible identity, lifecycle, steering, fan-out topology, joins, status, approvals, and artifacts.
- [ ] Terminal/process, files/diffs, Git, previews, and approvals are backend-owned, observable, reviewable, and leak-free.
- [ ] Retention, thread metadata/status, global guidance, provider sync/drift, and project affinity meet their default/lock/recovery rules.
- [ ] Skill and MCP registries enforce scope, compatibility, approval, trust, secrets/auth, lifecycle, sync/drift, and ledgered invocation.
- [ ] Electron control/stream transports negotiate versions, resume cursors, heartbeat, ACK, bounded buffering, and transport equivalence.
- [ ] Temporary workspace/Git/SQLite integration, property/state-machine, load, architecture-boundary, and surface normalization suites pass.
- [ ] A clean checkout passes formatting, lint, typecheck, unit, conformance, integration, property, and release gates with no unapproved live dependency.

## Current Verification Snapshot

`corepack pnpm run validate` passes on the current baseline: formatting, lint, typecheck, and 2 Vitest files containing 12 tests. That green result is not completion of this matrix; it is the starting gate for M0.
